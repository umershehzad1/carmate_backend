"use strict";

module.exports = {
  async up(queryInterface, Sequelize) {
    // Table is already named Notifications, skip this migration
  },

  async down(queryInterface, Sequelize) {
    // No changes needed
  },
};
