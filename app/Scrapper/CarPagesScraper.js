"use strict";

// CarPagesScraper has been permanently removed.
// This file intentionally contains a throwing stub so any lingering requires
// fail fast and developers are prompted to remove references.

class CarPagesScraperRemoved {
  constructor() {
    throw new Error(
      "CarPagesScraper has been removed from the codebase. Remove any requires to this file."
    );
  }
}

module.exports = CarPagesScraperRemoved;
